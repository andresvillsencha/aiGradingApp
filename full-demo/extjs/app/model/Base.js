/**
 * Base data model for the application.
 *
 * Defines the shared model schema namespace used by EvalApp model classes.
 */
Ext.define('EvalApp.model.Base', {
    extend: 'Ext.data.Model',

    schema: {
        namespace: 'EvalApp.model'
    }
});
