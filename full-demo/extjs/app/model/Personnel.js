/**
 * Model representing a personnel record used by the sample personnel grid.
 */
Ext.define('EvalApp.model.Personnel', {
    extend: 'EvalApp.model.Base',

    fields: [
        'name', 'email', 'phone'
    ]
});
